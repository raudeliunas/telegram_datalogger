# Criar um arquivo .env com base no arquivo .env-sample
# Colocar o token do Telegram no arquivo .env
# Colocar o caminho do log no arquivo .env
# Transformar seu script em executavel. Por exemplo: chmod +x acessoremoto.sh
# Colocar o que irá rodar no script bash
# Rodar o docker-compose up -d para iniciar